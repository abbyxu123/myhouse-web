# AGENTS.md

本文件适用于整个仓库，供 Codex、其他 AI 助手与未来开发者遵循。

## 开始任何工作之前

1. 阅读 `docs/plans/2026-09-07-starlight-mansion-website-design.md`。
2. 阅读 `docs/CONTENT_AND_ASSET_GUIDE.md`。
3. 检查 Git 状态，保留用户已有修改，不覆盖无关文件。
4. 先确认当前任务属于设计、内容、工程还是发布，不擅自扩展范围。

## 当前阶段

- 空间与体验设计已确认。
- 工程脚手架尚未建立。
- 在实施计划完成前，不引入重型 3D、CMS、数据库或开放式 AI 对话。
- 首个工程里程碑应先验证“玄关 → 客厅 → 快速认识 → 联系”这条完整路径。

## 不可破坏的体验原则

- 网站是“可探索的个人宅邸”，不是传统画册、房产 VR、玩具屋或完整游戏。
- 房间内部保持客人眼睛高度与生活重量；悬浮横切面只用于总览和转场。
- 核心信息始终可以直接读取，彩蛋只补充性格与故事。
- 桌面端左侧总控板持续可见；移动端必须提供等价的速览、探索与联系入口。
- 视觉采用温暖高级住宅、克制梦核与隐入家具的科技感，不使用赛博朋克霓虹或通用悬浮卡片堆叠。
- 所有关键内容必须存在于可访问的 HTML 中，不能只烤进图片。

## 事实、隐私与公开审批

- 不得虚构项目、荣誉、年份、客户、职位、合作结果、城市办公室或个人经历。
- “徐贝贝 Abby”与证书姓名“徐圣琦”的公开关系尚待本人最终确认。
- 公司的正式英文名称尚待本人最终确认；确认前不得自行翻译成正式对外名称。
- 手机号不公开。
- 个人素材、证书、客户 Logo、联系方式与收藏品只有在明确标记为 `approvedForPublic: true` 后才能进入公开页面和仓库。
- 不得提交合同、报价、未公开客户、NDA 内容、住址、私人关系、聊天记录或其他敏感信息。
- 出现其他人的照片必须确认展示许可，或先裁切、模糊、替换。

## 内容与多语言

- 页面结构、视觉组件和内容数据必须分离。
- 不在组件中写死七套语言文案。
- 所有语言共用稳定的实体 ID、项目关系、年份、媒体与热点坐标。
- 中文是事实底稿；英文是国际化改写；西、法、意、日、韩同时对照中英文事实。
- 修改事实底稿时必须同步更新 `sourceVersion`，并将受影响译文标记为待复核。
- 项目必须清楚呈现 `role`、`contribution` 与 `outcome`。
- 荣誉必须记录官方名称、主办方、年份、结果与核实状态。

## 素材规则

- 原始高清照片、证书扫描、PSD、AI、Keynote、PPT、HEIC、视频母版和压缩包只保存在 `materials-private/` 或其他私人存储中。
- 仓库只提交去除元数据、裁切、压缩并确认公开的网页衍生图。
- 不直接在页面中加载未经优化的大图。
- 素材命名使用小写英文和短横线；规范见 `docs/CONTENT_AND_ASSET_GUIDE.md`。
- 客户 Logo 和商标仅用于准确说明公开合作，不得上传完整品牌素材包。
- AI 生成或第三方素材必须记录来源、生成方式与可用权利。

## 安全与密钥

- 绝不提交 `.env`、API Key、Vercel Token、私钥或账户凭据。
- 浏览器可见的变量才能使用 `NEXT_PUBLIC_` 前缀。
- AI Abby 的服务端密钥只能保存在部署平台环境变量中。
- AI Abby 首发只使用审核后的结构化问答；开放式问答必须另行完成隐私、限流、来源和失败回退设计。

## 工程质量

- 优先使用预渲染非写实 3D 场景、2.5D 分层与 CSS／轻量动画，不默认引入持续运行的 WebGL 世界。
- 路由必须支持深层链接、刷新、分享、浏览器前进后退与原位置语言切换。
- 任何关键功能必须同时验证桌面、手机、键盘操作与减少动态模式。
- 热点点击区域不得小于 44 × 44 px。
- 动画必须遵守性能预算，并为 `prefers-reduced-motion` 和 `Save-Data` 提供降级。
- 新功能先写可验证的测试或验收条件；完成后运行与改动范围相称的检查。

## Git 工作流

- 重大功能在 `codex/` 前缀分支中完成，经过验证后再合入 `main`。
- 提交保持小而清晰，使用 `docs:`、`feat:`、`fix:`、`test:`、`chore:` 等前缀。
- 不重写已推送历史，不强推，不使用破坏性重置。
- 新增公开个人内容前先核实；正常代码、文档和已批准素材可以按计划提交。
